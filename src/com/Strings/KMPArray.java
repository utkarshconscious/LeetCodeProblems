package com.Strings;

public class KMPArray {

   /* public int [] createKMPArray(String input){
        char [] arr = input.toCharArray();
        int [] kmp = new int[arr.length];
        kmp[0]=0;
        int i=0;
        int j=1;
        while (i<arr.length){
            if(arr[i]==arr[j])
               kmp[i]=kmp[i-1]+1;
            else{

            }
        }
    }*/
}
